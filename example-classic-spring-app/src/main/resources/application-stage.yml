server:
  port: 8090
spring:
  h2:
    console:
      enabled: true
      path: /auth/h2
  datasource:
    url: jdbc:h2:file:./db/db-stage
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create

